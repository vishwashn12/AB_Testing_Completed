# Environment
NODE_ENV=development

# Server
PORT=5000

# Database
MONGODB_URI=mongodb://localhost:27017/ab-testing

# JWT Authentication (US-19)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=24h

# Security (US-20)
# HTTPS/SSL Configuration
# - For DEVELOPMENT: Set ENABLE_HTTPS=false (use HTTP) OR generate self-signed certificate
# - For PRODUCTION: Use Let's Encrypt or commercial SSL certificate
#
# To generate self-signed certificate for DEVELOPMENT testing:
#   Windows PowerShell:
#     New-SelfSignedCertificate -DnsName "localhost" -CertStoreLocation "cert:\LocalMachine\My"
#     (Then export .pfx file and convert to .pem/.key using OpenSSL)
#
#   Linux/Mac (using OpenSSL):
#     openssl req -x509 -newkey rsa:4096 -keyout ssl/key.pem -out ssl/cert.pem -days 365 -nodes
#
# For PRODUCTION with Let's Encrypt (free SSL):
#   1. Install Certbot: https://certbot.eff.org/
#   2. Run: certbot certonly --standalone -d yourdomain.com
#   3. Certificates will be in: /etc/letsencrypt/live/yourdomain.com/
#   4. Set SSL_CERT_PATH=/etc/letsencrypt/live/yourdomain.com/fullchain.pem
#   5. Set SSL_KEY_PATH=/etc/letsencrypt/live/yourdomain.com/privkey.pem
#
ENABLE_HTTPS=false
SSL_CERT_PATH=./ssl/cert.pem
SSL_KEY_PATH=./ssl/key.pem

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS
CORS_ORIGIN=http://localhost:5173

# Logging
LOG_LEVEL=debug

# Performance (US-22)
MONGODB_MAX_POOL_SIZE=50
ENABLE_COMPRESSION=true

# Analytics (US-12)
ENABLE_METRICS_CRON=true
METRICS_CRON_SCHEDULE=0 */5 * * * *

# User ID Hashing (US-21)
HASH_ALGORITHM=sha256
HASH_SALT=your-hash-salt-change-this
